<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Базов стил за вашите TextViews -->
    <style name="TextAppearance.App.TextView" parent="TextAppearance.AppCompat">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/textColor</item>
    </style>

    <!-- Базов стил за заглавния TextView -->
    <style name="TextAppearance.App.TextView.Bold" parent="TextAppearance.App.TextView">
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">25sp</item>
    </style>

    <!-- Стил за бутоните -->
    <style name="Widget.App.Button" parent="Widget.AppCompat.Button">
        <item name="android:textColor">@color/buttonTextColor</item>
        <item name="android:background">@drawable/button_background</item>
        <item name="android:padding">16dp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textSize">18sp</item>
    </style>
</resources>
